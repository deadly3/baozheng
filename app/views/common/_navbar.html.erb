<!-- <<<<<<< HEAD
<nav id="header" class="navbar navbar-fixed-top">
    <div id="header-container" class="container navbar-container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            </button>
            <a id="brand" class="navbar-brand" href="/">
              <img id="logo" src="http://ww1.sinaimg.cn/large/65e4f1e6jw1f9v6y3frsuj209b09bq2w.jpg">
            </a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-left">
                <% unless current_user && current_user.is_designer && !current_user.is_admin%>
                  <li> <%= link_to("➕ 整理房间！", new_request_path) %> </li>
                <% end %>
                <li> <%= link_to("📦 看作品", works_path) %> </li>
                <% if current_user && current_user.is_designer?%>
                  <li> <%= link_to("🙋🏻 抢订单", requests_path) %> </li>
                <% end %>
                <li> <%= link_to("达人广场", designers_path) %> </li>
======= -->
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <!-- <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button> -->
      <a class="navbar-brand" href="/">包整</a>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-left">
          <% unless current_user && current_user.is_designer && !current_user.is_admin%>
            <li> <%= link_to("⭕️ 整理房间！", new_request_path) %> </li>
          <% end %>
          <li> <%= link_to("⭕️ 看作品", works_path) %> </li>
          <% if current_user && current_user.is_designer?%>
            <li> <%= link_to("⭕️ 抢订单", requests_path) %> </li>
          <% end %>
          <li> <%= link_to("⭕️ 达人广场", designers_path) %> </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if !current_user %>
            <li><%= link_to("免费注册", new_user_registration_path) %> </li>
            <li><%= link_to("会员登入", new_user_session_path) %></li>
          <% else %>

            <% if !current_user.is_admin?%>
              <li><%= link_to("🗂 我的订单", account_requests_path(current_user.token)) %> </li>
            <% end %>
            <li><%= link_to("💌 我的消息", account_conversations_path) %> </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  <% if current_user.nickname.present?%>
                     <%= current_user.nickname %>
                  <% else %>
                    <%= current_user.email %>
                  <% end %>
                  <b class="caret"></b>
              </a>


              <ul class="dropdown-menu">

                <li><%= link_to("😎 用户中心", account_user_path(current_user.token)) %></li>
                <li role="separator" class="divider"></li>
                <% if current_user.is_designer? %>
                  <li> <%= link_to("🐼 达人后台", designers_path) %> </li>
                  <li role="separator" class="divider"></li>
                <% end %>
                <% if current_user.is_admin? %>
                  <li> <%= link_to("🐮 管理員后台", admin_users_path) %> </li>
                  <hr>
                <% end %>
                <li> <%= link_to("⛔️ 登出", destroy_user_session_path, method: :delete) %> </li>
               </ul>
            </li>
          <% end %>
        </li>

      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>
🐼
