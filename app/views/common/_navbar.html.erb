<!-- <nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <!-- <div class="navbar-header">
            <a class="navbar-brand" href="/">
          <img src="http://ww2.sinaimg.cn/large/65e4f1e6jw1f9tpq71wkwj208o089jrq.jpg" style="width: 60px" "height: 60px">
            </a>
        </div> -->




        <!-- Second navbar for sign in -->
         <nav class="navbar navbar-default">
           <div class="container">
             <!-- Brand and toggle get grouped for better mobile display -->
             <div class="navbar-header">
               <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-2">

               </button>
               <a class="navbar-brand" href="/">
                 <img src="http://ww1.sinaimg.cn/large/65e4f1e6jw1f9v6y3frsuj209b09bq2w.jpg" style="width: 40px" "height: 40px">
               </a>
               <ul class="nav navbar-nav navbar-left">
                 <li>
                   <%= link_to("➕ 发订单", new_request_path) %>
                 </li>
                 <li>
                   <%= link_to("📦 看作品", works_path) %>
                 </li>
                 <li>
                   <%= link_to("🙋🏻 抢订单", requests_path) %>
                 </li>
               </ul>
             </div>

             <!-- Collect the nav links, forms, and other content for toggling -->
             <div class="collapse navbar-collapse" id="navbar-collapse-2">
                <ul class="nav navbar-nav navbar-right">
                    <li>
                      <% if !current_user %>
                        <li><%= link_to("免费注册", new_user_registration_path) %> </li>
                        <li><%= link_to("会员登入", new_user_session_path) %></li>
                      <% else %>
                        <li><%= link_to("🗂 我的订单", account_requests_path(current_user.token)) %> </li>
                        <li><%= link_to("💌 我的消息", conversations_path) %> </li>
                        <li class="dropdown">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                              <% if current_user.nickname.present?%>
                                Hi!, <%= current_user.nickname %>
                              <% else %>
                                Hi!, <%= current_user.email %>
                              <% end %>
                              <b class="caret"></b>
                          </a>
                          <ul class="dropdown-menu">
                            <li><%= link_to("用户中心", account_user_path(current_user.token)) %></li>
                            <br>
                            <% if current_user.admin? %>
                            <li> <%= link_to("达人后台", admin_works_path) %> </li>
                            <hr>
                            <% end %>
                            <li> <%= link_to("登出", destroy_user_session_path, method: :delete) %> </li>
                           </ul>
                        </li>
                      <% end %>
                    </li>
                </ul>


               <!-- <div class="collapse nav navbar-nav nav-collapse slide-down" id="nav-collapse2">
                 <form class="navbar-form navbar-right form-inline" role="form">
                   <div class="form-group">
                     <label class="sr-only" for="Email">Email</label>
                     <input type="email" class="form-control" id="Email" placeholder="Email" autofocus required />
                   </div>
                   <div class="form-group">
                     <label class="sr-only" for="Password">Password</label>
                     <input type="password" class="form-control" id="Password" placeholder="Password" required />
                   </div>
                   <button type="submit" class="btn btn-success">Sign in</button>
                 </form>
               </div> -->


             </div><!-- /.navbar-collapse -->
           </div><!-- /.container -->
         </nav><!-- /.navbar -->












        <!-- Collect the nav links, forms, and other content for toggling -->
        <!-- <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li>
                  <% if !current_user %>
                    <li><%= link_to("注册", new_user_registration_path) %> </li>
                    <li><%= link_to("登入", new_user_session_path) %></li>
                  <% else %>
                    <li><%= link_to("用户中心🕴", account_user_path(current_user)) %> </li>
                    <li> <%= link_to(content_tag(:i, "我的邮箱", class: "fa fa-envelope"), conversations_path) %> </li>
                    <li class="dropdown">
                      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                          <% if current_user.nickname.present?%>
                            Hi!, <%= current_user.nickname %>
                          <% else %>
                            Hi!, <%= current_user.email %>
                          <% end %>
                          <b class="caret"></b>
                      </a>
                      <ul class="dropdown-menu">
                        <li>
                          <%= link_to(content_tag(:i, " 用户中心", class: "fa fa-user"), account_requests_path(current_user)) %>
                        </li>

                        <% if current_user.admin? %>
                        <br>
                          <li>
                              <%= link_to(content_tag(:i, " 管理员中心", class: "fa fa-user-circle"), admin_works_path) %>
                          </li>
                        <% end %>
<<<<<<< HEAD

                        <hr>

                        <li>
                          <%= link_to(content_tag(:i, " 登出", class: "fa fa-sign-out"), destroy_user_session_path, data: { confirm: "Are you sure?" }, method: :delete) %>
                        </li>
=======
                        <li> <%= link_to("登出", destroy_user_session_path, data: { confirm: "Are you sure?" }, method: :delete) %> </li>
>>>>>>> develop
                      </ul>
                    </li>
                  <% end %>
                </li>
            </ul>
        </div> -->
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
